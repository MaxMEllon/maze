#ifndef INCLUDED_ENV
#define INCLUDED_ENV

#include <iostream>
#include <time.h>
#include <stdlib.h>
#include <ncurses.h>

typedef enum _PLACE_TYPE {
  ENPTY,
  WALL,
  GALL,
  CHARACTER
} PLACE_TYPE;

#endif
